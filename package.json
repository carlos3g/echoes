{
  "name": "echoes",
  "version": "0.0.1",
  "private": true,
  "description": "",
  "license": "MIT",
  "author": "carlos3g",
  "scripts": {
    "prepare": "husky",
    "style": "lerna run style",
    "dev": "lerna run dev",
    "db": "lerna run db:generate && lerna run db:migrate && lerna run db:seed",
    "db:test": "lerna run db:generate && lerna run db:migrate:test"
  },
  "devDependencies": {
    "@commitlint/cli": "^19.6.1",
    "@commitlint/config-conventional": "^19.6.0",
    "husky": "^9.1.7",
    "jest": "^29.5.0",
    "lerna": "^8.1.9",
    "lint-staged": "^15.2.11"
  },
  "workspaces": [
    "packages/api",
    "packages/webapp",
    "apps/*"
  ]
}
