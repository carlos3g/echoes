meta {
  name: Create Tag
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/v1/tags
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "title": "My New Tag"
  }
}
