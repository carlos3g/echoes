meta {
  name: Tag Author
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/v1/authors/:uuid/tag
  body: json
  auth: bearer
}

params:path {
  uuid: author-uuid-here
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "tagUuid": "tag-uuid-here"
  }
}
